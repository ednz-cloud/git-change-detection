playbooks/bootstrap.yml:
  triggers:
    ansible/playbooks:
      - bootstrap.yml
      - tasks/crowdsec.yml

playbooks/k3s_cluster.yml:
  depends_on: "playbooks/bootstrap.yml" # should be a list, not a string
  triggers:
    ansible/playbooks:
      - k3s_cluster.yml

blacklist:
  - playbooks/netbird_controller.yml
